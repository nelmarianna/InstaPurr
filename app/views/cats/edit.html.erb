<%= simple_form_for @cat do |f| %>
  <%= f.input :caption, :label => "\"cat\"tion" %>
  <%= f.button :submit %>
<% end %>
<p>
<%= link_to "Delete my cat", cat_path(@cat), method: :delete, data: { confirm: "Are you sure you want to delete this post?" } %>
|
<%= link_to "Cancel", cats_path %></p>